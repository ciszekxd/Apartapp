This version of Project has all functionalities 
(however may still need a few changes), 
to run it correctly you need to adjust database URL in .env,
also there is a database example in project files